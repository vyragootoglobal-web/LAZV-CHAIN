RUN NODE (ULTRA SIMPLE) Requirements: Python 3.9+ pip Android / PC / Linux / Mac Steps: # 1. Clone repository git clone https://github.com/vyragootoglobal-web/LAZV-CHAIN.git cd LAZV-CHAIN # 2. Install dependencies pip install -r requirements.txt # 3. Run node python node.py Access local chain state: http://127.0.0.1:5000/chain HP Light Node Tips: Limit peers: 5–10 Auto sleep if battery <30% Light sync only, no archive Node relay + peer discovery + monitoring 📌 10 HP light nodes > 1 VPS server. Don't be afraid that the HP node is just a relay, it already helps the chain "live".
